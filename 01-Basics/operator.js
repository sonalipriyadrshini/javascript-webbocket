/*-Arithmatic operator(+,-.*,/,%,**(exponent),++,--)
-Assignment opertor(=,+=,-=,/=,%=)
-comparison(==(only check values),===(check value as well as datatype also),!=,!==,>,,,<=,>=)
-logic(&&,||,!)
-type(typeof,instanceof(return true if an object is an instance an object type))
-bitwise(&,|,~,^,<<,>>,>>>) */